if ($('input#subscribe-button').val() == 'Subscribe') {
  $('input#subscribe-button').val('Unsubscribe')
                             .addClass('btn-default')
                             .removeClass('btn-primary');
} else {
  $('input#subscribe-button').val('Subscribe')
                             .addClass('btn-primary')
                             .removeClass('btn-default');
}
