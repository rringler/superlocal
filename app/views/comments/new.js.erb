if ($("<%= "div#comment-#{@comment.parent_id}-reply" %>").html() == '') {
  $("<%= "div#comment-#{@comment.parent_id}-reply" %>").html("<%= j render partial: 'comments/form', locals: { comment: @comment } %>");
} else {
  $("<%= "div#comment-#{@comment.parent_id}-reply" %>").html('');
}
